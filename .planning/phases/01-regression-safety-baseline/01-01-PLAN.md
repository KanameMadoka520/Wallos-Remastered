---
phase: 01-regression-safety-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/regression_runner.php
  - tests/lib/regression_bootstrap.php
  - tests/lib/regression_http.php
  - tests/lib/regression_output.php
autonomous: true
requirements:
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - A single regression CLI entrypoint accepts base URL and authentication inputs from CLI flags or environment variables.
    - Shared HTTP helpers can persist cookies and expose status, headers, and body details for smoke assertions.
    - The regression runner prints a structured pass/fail/skip summary and exits non-zero on failure.
  artifacts:
    - tests/regression_runner.php
    - tests/lib/regression_bootstrap.php
    - tests/lib/regression_http.php
    - tests/lib/regression_output.php
  key_links:
    - tests/regression_runner.php loads normalized config from tests/lib/regression_bootstrap.php
    - tests/regression_runner.php routes all network access through tests/lib/regression_http.php
    - tests/regression_runner.php renders final results through tests/lib/regression_output.php
---

<objective>
Build the regression harness foundation for Phase 1.

Purpose: Establish the single CLI entrypoint, config loading, cookie-aware HTTP helpers, and structured output layer that all smoke checks will use.
Output: A reusable regression runner foundation under `tests/` that can be extended with public and authenticated checks without hard-coding environment-specific secrets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-regression-safety-baseline/01-CONTEXT.md
@tests/budget_regression_test.php
@tests/payment_ledger_test.php
@tests/subscription_preferences_test.php
@login.php
@includes/checksession.php
@includes/connect_endpoint.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create regression bootstrap and configuration parsing</name>
  <files>tests/regression_runner.php, tests/lib/regression_bootstrap.php</files>
  <read_first>.planning/phases/01-regression-safety-baseline/01-CONTEXT.md, tests/budget_regression_test.php</read_first>
  <action>Create a bootstrap layer that normalizes runtime configuration for the future smoke suite. Support CLI flags and environment fallbacks for at least `--base-url`, `--cookie`, `--username`, `--password`, `--public-only`, `--auth-only`, `--existing-only`, `--list`, and `--help`, with matching env vars such as `WALLOS_BASE_URL`, `WALLOS_TEST_COOKIE`, `WALLOS_TEST_USERNAME`, and `WALLOS_TEST_PASSWORD`. Normalize the base URL, reject empty/invalid URLs with a clear message, and avoid hard-coded credentials or machine-specific paths. Make the runner expose a stable config object that downstream checks can consume without re-parsing argv.</action>
  <verify>php -l tests/regression_runner.php && php -l tests/lib/regression_bootstrap.php</verify>
  <acceptance_criteria>
    - `tests/lib/regression_bootstrap.php` contains parsing for `WALLOS_BASE_URL`, `WALLOS_TEST_COOKIE`, `WALLOS_TEST_USERNAME`, and `WALLOS_TEST_PASSWORD`
    - `tests/regression_runner.php` contains handlers for `--help` and `--list`
    - Invalid or missing base URLs produce a deterministic human-readable error instead of a PHP warning or fatal
  </acceptance_criteria>
  <done>Bootstrap config exists, lint passes, and the runner can start without touching any smoke-specific logic yet.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared cookie-aware HTTP helpers</name>
  <files>tests/lib/regression_http.php</files>
  <read_first>includes/checksession.php, includes/connect_endpoint.php, login.php</read_first>
  <action>Implement a lightweight HTTP helper using standard PHP facilities only. It must support GET/POST requests, cookie jar persistence, redirect capture, request headers, response headers, body extraction, and status-code reporting. Add helper functions to detect the `wallos_login` cookie, decode JSON bodies safely, and perform basic HTML substring/meta-tag assertions without introducing browser automation or external packages. Keep this layer generic so Phase 2+ can reuse it for future smoke scenarios.</action>
  <verify>php -l tests/lib/regression_http.php</verify>
  <acceptance_criteria>
    - `tests/lib/regression_http.php` exposes request helpers for both GET and POST
    - Cookie persistence is implemented in shared helpers rather than duplicated in the runner
    - JSON decode failures return structured assertion data instead of raw warnings
  </acceptance_criteria>
  <done>The HTTP helper can be called by the runner and is capable of preserving login/session cookies for later authenticated checks.</done>
</task>

<task type="auto">
  <name>Task 3: Add structured summary and exit-code behavior</name>
  <files>tests/regression_runner.php, tests/lib/regression_output.php</files>
  <read_first>tests/payment_ledger_test.php, tests/subscription_preferences_test.php</read_first>
  <action>Add a shared reporting layer that records PASS / FAIL / SKIP results with per-check names, short details, and a final aggregate summary. Make the runner return exit code `0` only when every executed check passes, return a non-zero code when any check fails, and distinguish skipped authenticated checks from real failures. Add a `--list` mode that prints the available suites/checks without running them, and ensure output stays concise and CLI-friendly rather than dumping raw HTML or full bodies.</action>
  <verify>php tests/regression_runner.php --help && php tests/regression_runner.php --list</verify>
  <acceptance_criteria>
    - `tests/lib/regression_output.php` contains PASS / FAIL / SKIP rendering helpers
    - `tests/regression_runner.php` exits non-zero when a simulated failure is returned by the result aggregator
    - `php tests/regression_runner.php --list` prints named suites/checks without requiring credentials
  </acceptance_criteria>
  <done>The regression foundation can be invoked from one command, produces a stable summary, and is ready for concrete smoke checks in the next plan.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php -l tests/regression_runner.php`
- [ ] `php -l tests/lib/regression_bootstrap.php`
- [ ] `php -l tests/lib/regression_http.php`
- [ ] `php -l tests/lib/regression_output.php`
- [ ] `php tests/regression_runner.php --list`
- [ ] Mirror the changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos` with `docker compose -f docker-compose.yaml up -d --build --pull never`
- [ ] Verify `http://127.0.0.1:18282/health.php` returns `OK`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No hard-coded credentials or machine-specific secrets introduced
- A single CLI regression entrypoint exists and is reusable by later smoke suites
- Structured summary and exit-code semantics are implemented for future regression growth
  </success_criteria>

<output>
After completion, create `.planning/phases/01-regression-safety-baseline/01-01-SUMMARY.md`
</output>
