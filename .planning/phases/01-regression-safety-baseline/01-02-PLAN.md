---
phase: 01-regression-safety-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/regression_runner.php
  - tests/lib/regression_checks.php
  - tests/lib/regression_legacy.php
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
user_setup:
  - Provide either `WALLOS_TEST_COOKIE` or `WALLOS_TEST_USERNAME` + `WALLOS_TEST_PASSWORD` when you want the authenticated smoke suite to run instead of being skipped.
must_haves:
  truths:
    - The regression runner can validate public smoke endpoints for `health.php`, `login.php`, and `registration.php`.
    - The runner can validate `theme-color` output and basic service-worker registration/cache-contract signals without a browser E2E stack.
    - The runner can execute authenticated smoke checks for subscription page endpoints by reusing a cookie or logging in with supplied credentials.
    - The runner can invoke the existing PHP regression tests and include their pass/fail status in the unified summary.
  artifacts:
    - tests/lib/regression_checks.php
    - tests/lib/regression_legacy.php
    - tests/regression_runner.php
  key_links:
    - tests/regression_runner.php dispatches public, authenticated, and legacy suites through dedicated helper modules
    - tests/lib/regression_checks.php uses the shared HTTP/config foundation created by 01-01
    - tests/lib/regression_legacy.php wraps existing PHP regression tests into the unified summary/exit-code flow
---

<objective>
Implement the actual smoke checks and legacy-test aggregation for Phase 1.

Purpose: Turn the harness foundation into a useful regression entrypoint that covers the highest-risk public pages, authenticated pagination endpoints, and the already-existing PHP logic tests in one summary.
Output: A runnable smoke suite that exercises public pages, authenticated endpoint contracts, and wrapped legacy tests from a single command.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-regression-safety-baseline/01-CONTEXT.md
@.planning/phases/01-regression-safety-baseline/01-01-PLAN.md
@health.php
@login.php
@registration.php
@scripts/all.js
@service-worker.js
@includes/header.php
@scripts/subscription-pages.js
@scripts/subscriptions.js
@includes/subscription_pages.php
@endpoints/subscriptionpages.php
@endpoints/subscriptions/get.php
@tests/budget_regression_test.php
@tests/payment_ledger_test.php
@tests/subscription_preferences_test.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add public-page, theme-color, and service-worker smoke checks</name>
  <files>tests/lib/regression_checks.php, tests/regression_runner.php</files>
  <read_first>health.php, login.php, registration.php, includes/header.php, scripts/all.js, service-worker.js</read_first>
  <action>Implement a public smoke suite that requests `health.php`, `login.php`, and `registration.php` from the configured base URL and verifies stable contracts instead of brittle full-page snapshots. Check that `health.php` returns HTTP 200 with body `OK`, that login/registration respond successfully and contain a `meta[name="theme-color"]`, and that service-worker wiring still exists by validating the registration call in `scripts/all.js` plus the cache constant pattern in `service-worker.js`. Keep these assertions tolerant of text/content style changes, but strict about the contract signals that would indicate real regressions.</action>
  <verify>php tests/regression_runner.php --base-url=http://127.0.0.1:18282 --public-only</verify>
  <acceptance_criteria>
    - `tests/lib/regression_checks.php` contains explicit smoke checks for `health.php`, `login.php`, and `registration.php`
    - The public suite checks for `meta[name="theme-color"]` on login and registration responses
    - The public suite verifies `scripts/all.js` still registers `service-worker.js`
    - The public suite verifies `service-worker.js` still declares cache constants such as `static-cache-v`
  </acceptance_criteria>
  <done>The public suite can catch the most common regressions in public entry pages and cache wiring from one command.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated subscription-page endpoint smoke checks</name>
  <files>tests/lib/regression_checks.php, tests/regression_runner.php</files>
  <read_first>login.php, includes/connect_endpoint.php, endpoints/subscriptionpages.php, endpoints/subscriptions/get.php, scripts/subscriptions.js, scripts/subscription-pages.js</read_first>
  <action>Implement an authenticated suite that first tries to reuse a supplied `wallos_login` cookie and otherwise logs in with supplied username/password, then performs smoke checks against the highest-risk pagination endpoints. Cover at least: unauthenticated `endpoints/subscriptions/get.php` returns a clean 401/session-expired contract, authenticated `endpoints/subscriptionpages.php` returns JSON with `success`, `message`, `pages`, and `counts`, and authenticated `endpoints/subscriptions/get.php?subscription_page=all` returns non-empty HTML rather than warnings or dumped code. If auth inputs are absent, mark the authenticated suite as SKIP with a clear reason rather than FAIL.</action>
  <verify>php tests/regression_runner.php --base-url=http://127.0.0.1:18282 --auth-only</verify>
  <acceptance_criteria>
    - Auth flow supports either `WALLOS_TEST_COOKIE` or username/password login
    - Missing auth input causes a visible SKIP message, not a generic error
    - `endpoints/subscriptionpages.php` is validated as JSON with expected top-level keys
    - `endpoints/subscriptions/get.php?subscription_page=all` is validated for authenticated HTML response and clean unauthenticated 401 behavior
  </acceptance_criteria>
  <done>The authenticated suite can catch the exact pagination/session regressions that motivated Phase 1 without requiring browser automation.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap existing PHP regression tests into the unified runner</name>
  <files>tests/lib/regression_legacy.php, tests/regression_runner.php</files>
  <read_first>tests/budget_regression_test.php, tests/payment_ledger_test.php, tests/subscription_preferences_test.php</read_first>
  <action>Add a legacy-test wrapper that executes the existing PHP regression scripts as subprocesses, captures their exit codes/output, and reports them through the same PASS / FAIL / SKIP summary used by the smoke checks. Preserve the current test files as the source of truth unless a minimal compatibility tweak is genuinely required. Expose a dedicated mode such as `--existing-only` or `--run-existing` so operators can run the pre-existing logic checks alone or as part of the full suite.</action>
  <verify>php tests/regression_runner.php --existing-only</verify>
  <acceptance_criteria>
    - `tests/lib/regression_legacy.php` runs all three existing PHP tests through a shared wrapper
    - `tests/regression_runner.php` exposes a dedicated mode for legacy-only execution
    - Legacy failures are surfaced in the unified summary with non-zero exit code behavior
  </acceptance_criteria>
  <done>One regression command can now cover both existing logic tests and new smoke checks, satisfying the Phase 1 baseline.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php tests/regression_runner.php --base-url=http://127.0.0.1:18282 --public-only`
- [ ] `php tests/regression_runner.php --existing-only`
- [ ] `php tests/regression_runner.php --auth-only` (expect PASS or explicit SKIP when auth inputs are absent)
- [ ] `php -l tests/lib/regression_checks.php`
- [ ] `php -l tests/lib/regression_legacy.php`
- [ ] Mirror the changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos` with `docker compose -f docker-compose.yaml up -d --build --pull never`
- [ ] Verify `http://127.0.0.1:18282/health.php` returns `OK`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Public smoke coverage exists for `health.php`, `login.php`, `registration.php`, `theme-color`, and service-worker contract signals
- Authenticated smoke coverage exists for subscription page endpoints with explicit PASS / FAIL / SKIP outcomes
- Existing PHP regression tests can be launched through the same runner and contribute to one final summary
  </success_criteria>

<output>
After completion, create `.planning/phases/01-regression-safety-baseline/01-02-SUMMARY.md`
</output>
