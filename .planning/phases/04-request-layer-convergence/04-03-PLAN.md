---
phase: 04-request-layer-convergence
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - scripts/calendar.js
  - tests/lib/regression_checks.php
autonomous: true
requirements:
  - FRON-02
user_setup: []
must_haves:
  truths:
    - remaining high-frequency calendar requests align with shared request helpers
    - regression baseline still passes after request-layer convergence
  artifacts:
    - scripts/calendar.js
  key_links:
    - calendar.js uses shared request helpers
    - regression runner remains green
---

<objective>
Finish the high-frequency request-layer convergence pass and keep regression green.

Purpose: Close out the first structural convergence phase with remaining calendar requests and safety checks.
Output: Calendar request alignment plus confirmed regression baseline after the convergence changes.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-request-layer-convergence/04-CONTEXT.md
@scripts/calendar.js
@tests/lib/regression_checks.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Align remaining high-frequency calendar requests</name>
  <files>scripts/calendar.js</files>
  <read_first>scripts/calendar.js, scripts/api.js</read_first>
  <action>Move remaining calendar requests that still use raw fetch onto shared WallosApi/WallosHttp patterns, preserving existing UI behavior.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\calendar.js</verify>
  <acceptance_criteria>
    - calendar request paths use shared request helpers
  </acceptance_criteria>
  <done>Calendar request paths align with the shared request layer.</done>
</task>
<task type="auto">
  <name>Task 2: Re-run regression baseline after request convergence</name>
  <files>tests/lib/regression_checks.php</files>
  <read_first>tests/regression_runner.php, tests/lib/regression_checks.php</read_first>
  <action>Only adjust regression helper code if necessary for compatibility, then re-run the full regression baseline to confirm request-layer convergence did not break the stable milestone foundations.</action>
  <verify>docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1</verify>
  <acceptance_criteria>
    - regression baseline remains green after request-layer changes
  </acceptance_criteria>
  <done>Request-layer convergence is verified against the existing regression baseline.</done>
</task>
</tasks>

<verification>
- [ ] `node --check scripts/calendar.js`
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1`
- [ ] Sync to deployment copies and rebuild Wallos
- [ ] Verify `health.php`
</verification>

<success_criteria>
- Remaining high-frequency request paths converge
- Regression baseline remains stable
  </success_criteria>

<output>
After completion, create `.planning/phases/04-request-layer-convergence/04-03-SUMMARY.md`
</output>
