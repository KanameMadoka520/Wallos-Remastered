---
phase: 05-subscriptions-modular-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - scripts/subscriptions.js
autonomous: true
requirements:
  - SUBM-02
user_setup: []
must_haves:
  truths:
    - subscription page still behaves the same after modular extraction
    - regression baseline remains green
  artifacts:
    - scripts/subscriptions.js
  key_links:
    - final subscription entrypoint remains behavior-compatible after modular extraction
---

<objective>
Do the final cleanup pass for the modularized subscription entrypoint and verify behavior stability.

Purpose: Ensure the modularization pass reduces complexity without destabilizing the page.
Output: Final cleanup in subscriptions.js plus green regression baseline.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-subscriptions-modular-refactor/05-CONTEXT.md
@scripts/subscriptions.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Final cleanup and regression confirmation</name>
  <files>scripts/subscriptions.js</files>
  <read_first>scripts/subscriptions.js, tests/regression_runner.php</read_first>
  <action>Remove any now-obsolete glue left behind by the modular extraction work, keeping the page stable, then re-run the regression baseline to confirm the subscription page refactor stayed compatible.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\subscriptions.js && docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1</verify>
  <acceptance_criteria>
    - subscriptions.js no longer carries dead glue from the extraction pass
    - regression baseline remains green
  </acceptance_criteria>
  <done>Subscription modularization closes with a clean entrypoint and passing regression baseline.</done>
</task>
</tasks>

<verification>
- [ ] `node --check scripts/subscriptions.js`
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1`
- [ ] Sync to deployment copies and rebuild Wallos
</verification>

<success_criteria>
- Subscription page modularization remains stable
  </success_criteria>

<output>
After completion, create `.planning/phases/05-subscriptions-modular-refactor/05-03-SUMMARY.md`
</output>
