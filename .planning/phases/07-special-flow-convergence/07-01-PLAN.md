---
phase: 07-special-flow-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/admin.js
  - scripts/subscriptions.js
autonomous: true
requirements:
  - SPCL-01
  - SPCL-02
user_setup: []
must_haves:
  truths:
    - admin cron text requests no longer rely on raw fetch for their main request path
    - subscriptions logo search no longer relies on raw fetch JSON parsing
    - regression baseline remains green
  artifacts:
    - scripts/admin.js
    - scripts/subscriptions.js
  key_links:
    - admin.js uses shared request text/json helpers for remaining special flows
    - subscriptions.js uses shared GET JSON helper for logo search
---

<objective>
Converge the remaining high-value special request flows onto shared request helpers.

Purpose: Shrink the last obvious request-layer drift without changing protocol semantics.
Output: admin cron/update special flows and subscription logo search aligned with shared request helpers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-special-flow-convergence/07-CONTEXT.md
@scripts/admin.js
@scripts/subscriptions.js
@scripts/api.js
</context>

<tasks>
<task type="auto">
  <name>Task 1: Converge admin special request flows</name>
  <files>scripts/admin.js</files>
  <read_first>scripts/admin.js, scripts/api.js</read_first>
  <action>Replace the remaining admin cron text request path and update-check trigger path with shared request helpers, preserving text semantics and fire-and-forget behavior where appropriate.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\admin.js</verify>
  <acceptance_criteria>
    - executeCronJob no longer uses raw fetch text
    - update-check trigger no longer uses naked fetch
  </acceptance_criteria>
  <done>Admin special flows use shared request helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Converge subscription logo search flow</name>
  <files>scripts/subscriptions.js</files>
  <read_first>scripts/subscriptions.js, scripts/api.js</read_first>
  <action>Replace the remaining raw logo-search GET JSON flow with the shared request helper while preserving the current popup/search behavior.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\subscriptions.js</verify>
  <acceptance_criteria>
    - logo search no longer uses raw fetch JSON parsing
  </acceptance_criteria>
  <done>Subscription logo search uses shared request helpers.</done>
</task>
</tasks>

<verification>
- [ ] `node --check scripts/admin.js`
- [ ] `node --check scripts/subscriptions.js`
- [ ] Sync to deployment copies and rebuild Wallos
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1`
</verification>

<success_criteria>
- High-value remaining special flows converge
  </success_criteria>

<output>
After completion, create `.planning/phases/07-special-flow-convergence/07-01-SUMMARY.md`
</output>
