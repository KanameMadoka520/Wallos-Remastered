---
phase: 02-session-and-401-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/auth_session.php
  - includes/connect_endpoint.php
  - includes/validate_endpoint.php
autonomous: true
requirements:
  - SESS-01
user_setup: []
must_haves:
  truths:
    - A shared backend helper owns session start, `wallos_login` cookie restoration, token validation, user status checks, and cookie cleanup.
    - Endpoint entrypoints can call the shared helper instead of duplicating login restoration logic.
    - Async endpoint validation can emit a standardized machine-readable session failure contract.
  artifacts:
    - includes/auth_session.php
    - includes/connect_endpoint.php
    - includes/validate_endpoint.php
  key_links:
    - includes/connect_endpoint.php loads and uses includes/auth_session.php as the source of truth
    - includes/validate_endpoint.php uses the shared endpoint contract helper instead of hand-rolling session failure responses
---

<objective>
Create the shared backend session/auth foundation for Phase 2.

Purpose: Remove the duplicated auth/session recovery core from endpoint code and establish a standardized session-failure contract that later plans can safely adopt.
Output: A shared backend helper plus updated endpoint validation primitives that reduce duplicated session logic and expose a stable async failure shape.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-and-401-unification/02-CONTEXT.md
@includes/connect_endpoint.php
@includes/validate_endpoint.php
@login.php
@logout.php
@includes/user_status.php
@includes/security_rate_limits.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared backend auth/session helper</name>
  <files>includes/auth_session.php, includes/connect_endpoint.php</files>
  <read_first>.planning/phases/02-session-and-401-unification/02-CONTEXT.md, includes/connect_endpoint.php, login.php, logout.php</read_first>
  <action>Create a shared helper file dedicated to backend session/auth restoration. Move the reusable core out of `connect_endpoint.php`: session bootstrap, cookie parsing, token validation, user loading, trashed-user detection, and login-cookie cleanup. Keep endpoint-specific behavior such as request logging and backend rate limiting in `connect_endpoint.php`, but make it consume the extracted helper instead of owning the core implementation. Preserve existing cookie/session semantics and do not silently change page-vs-endpoint output behavior yet.</action>
  <verify>docker exec wallos-local php -l /var/www/html/includes/auth_session.php && docker exec wallos-local php -l /var/www/html/includes/connect_endpoint.php</verify>
  <acceptance_criteria>
    - `includes/auth_session.php` becomes the source of truth for shared session restoration primitives
    - `includes/connect_endpoint.php` no longer contains its own full copy of cookie parsing/token validation logic
    - Shared helper includes explicit cookie-clear and user-status guard behavior
  </acceptance_criteria>
  <done>A reusable backend auth/session helper exists and endpoint bootstrap has been reduced to orchestration logic.</done>
</task>

<task type="auto">
  <name>Task 2: Define standardized async session failure contract</name>
  <files>includes/auth_session.php, includes/validate_endpoint.php, includes/connect_endpoint.php</files>
  <read_first>includes/validate_endpoint.php, endpoints/subscriptions/get.php, endpoints/subscription/paymenthistory.php</read_first>
  <action>Add helper functions for a consistent async session failure contract. Standardize the JSON shape for protected endpoint failures so later endpoint migrations can emit a stable structure such as `success: false`, a dedicated error code, a session-expired marker, and the translated message. Update `validate_endpoint.php` and any common helper surface in `connect_endpoint.php` so they expose the same contract rather than slightly different payloads or raw strings.</action>
  <verify>docker exec wallos-local php -l /var/www/html/includes/validate_endpoint.php</verify>
  <acceptance_criteria>
    - `includes/validate_endpoint.php` returns a standardized session-expired JSON payload
    - Shared helpers expose a stable machine-readable session failure marker or code
    - Contract helpers remain compatible with existing `session_expired` translation usage
  </acceptance_criteria>
  <done>The shared async session-failure shape is defined and common validation helpers emit it consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Keep Phase 1 regression runner aligned with backend foundation changes</name>
  <files>tests/lib/regression_checks.php</files>
  <read_first>tests/lib/regression_checks.php, .planning/phases/01-regression-safety-baseline/01-VERIFICATION.md</read_first>
  <action>Update the existing regression helper only where necessary so Phase 1’s baseline still checks the unauthenticated 401/session-expired contract after the helper extraction. Keep the checks contract-focused and do not over-expand them in this plan; just ensure the current smoke suite continues to describe the expected failure shape in a way that remains useful across the Phase 2 migration.</action>
  <verify>docker exec wallos-local php -l /var/www/html/tests/lib/regression_checks.php</verify>
  <acceptance_criteria>
    - Any changed 401/session-expired assertions remain aligned with the new shared contract helper
    - The regression check stays contract-oriented and does not become brittle to wording/layout noise
  </acceptance_criteria>
  <done>The existing regression baseline remains compatible with the new backend auth/session foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker exec wallos-local php -l /var/www/html/includes/auth_session.php`
- [ ] `docker exec wallos-local php -l /var/www/html/includes/connect_endpoint.php`
- [ ] `docker exec wallos-local php -l /var/www/html/includes/validate_endpoint.php`
- [ ] `docker exec wallos-local php -l /var/www/html/tests/lib/regression_checks.php`
- [ ] Mirror changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos` with `docker compose -f docker-compose.yaml up -d --build --pull never`
- [ ] Verify `http://127.0.0.1:18282/health.php` returns `OK`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shared backend auth/session helper exists and is used by endpoint bootstrap
- Async session failure contract is standardized in shared validation code
- Phase 1 regression checks still describe the protected endpoint failure contract coherently
  </success_criteria>

<output>
After completion, create `.planning/phases/02-session-and-401-unification/02-01-SUMMARY.md`
</output>
