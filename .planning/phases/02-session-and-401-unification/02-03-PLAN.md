---
phase: 02-session-and-401-unification
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - scripts/api.js
  - scripts/common.js
  - scripts/subscriptions.js
  - tests/lib/regression_checks.php
  - tests/regression_runner.php
autonomous: true
requirements:
  - SESS-02
  - SESS-03
user_setup: []
must_haves:
  truths:
    - Frontend request handling recognizes the unified session failure contract in a shared layer.
    - Business modules stop hand-parsing 401/session-expired behavior unless they need a small scene-specific hook.
    - Regression coverage proves the new contract still works from the Phase 1 runner.
  artifacts:
    - scripts/api.js
    - scripts/common.js
    - tests/lib/regression_checks.php
  key_links:
    - scripts/api.js surfaces a shared session-failure recognition path
    - scripts/subscriptions.js consumes the shared handling instead of owning raw 401 logic
    - regression checks validate the new endpoint contract shape
---

<objective>
Unify the frontend side of session-expired handling and align regression coverage with the new contract.

Purpose: Finish Phase 2 by moving session-failure recognition into the shared request layer, reducing duplicate business logic and proving the new contract through the existing regression runner.
Output: A shared frontend session-failure path plus updated regression checks that exercise the standardized protected endpoint behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-and-401-unification/02-CONTEXT.md
@.planning/phases/02-session-and-401-unification/02-02-PLAN.md
@scripts/api.js
@scripts/common.js
@scripts/subscriptions.js
@tests/regression_runner.php
@tests/lib/regression_checks.php
@.planning/phases/01-regression-safety-baseline/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared frontend session-failure recognition to the request layer</name>
  <files>scripts/api.js, scripts/common.js</files>
  <read_first>scripts/api.js, scripts/common.js, includes/validate_endpoint.php</read_first>
  <action>Extend the shared frontend request layer so it can recognize the standardized session failure contract introduced in Phase 2 backend work. Centralize the recognition logic in `scripts/api.js` (and, if needed, a small shared hook in `scripts/common.js`) so callers can tell the difference between session expiration, generic backend errors, and rate-limit/account-status failures without each module re-parsing raw status/message fields.</action>
  <verify>rg -n \"session_expired|sessionExpired|session_expired\" D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\api.js D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\common.js</verify>
  <acceptance_criteria>
    - Shared request code recognizes the unified session failure contract in one place
    - Session failure is distinguishable from generic request errors and rate-limit/account-state cases
    - The shared logic is reusable by multiple business modules
  </acceptance_criteria>
  <done>The frontend request layer has a single shared way to recognize session-expired conditions.</done>
</task>

<task type="auto">
  <name>Task 2: Remove hand-written 401 parsing from high-risk frontend modules</name>
  <files>scripts/subscriptions.js</files>
  <read_first>scripts/subscriptions.js, scripts/api.js</read_first>
  <action>Refactor high-risk frontend code, starting with `scripts/subscriptions.js`, so it uses the shared request/session-failure recognition path instead of manually checking raw 401 status or freeform `session_expired` semantics. Preserve any truly scene-specific reaction hooks such as refresh/reload behavior, but move raw detection into the shared request layer.</action>
  <verify>rg -n \"error\\?\\.status|response\\?\\.status|401\" D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\subscriptions.js</verify>
  <acceptance_criteria>
    - `scripts/subscriptions.js` no longer owns low-level raw 401/session contract parsing
    - Any remaining module logic is limited to scene-specific behavior after a shared session-failure decision
  </acceptance_criteria>
  <done>The highest-risk frontend module now consumes the shared session handling path instead of duplicating low-level detection logic.</done>
</task>

<task type="auto">
  <name>Task 3: Expand regression checks for the unified Phase 2 contract</name>
  <files>tests/lib/regression_checks.php, tests/regression_runner.php</files>
  <read_first>tests/lib/regression_checks.php, endpoints/subscriptions/get.php, endpoints/payments/get.php</read_first>
  <action>Update the Phase 1 regression suite so it verifies the new standardized protected-endpoint session failure contract rather than the old mixed behavior. Keep the checks compact and contract-oriented: at minimum, validate the unauthenticated protected-endpoint response shape for the key endpoints migrated in 02-02, and ensure the runner summary remains clear when those checks fail.</action>
  <verify>docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1 --auth-only</verify>
  <acceptance_criteria>
    - Regression checks assert the new standardized session failure contract for migrated endpoints
    - The auth suite still degrades to explicit SKIP when positive-path credentials are absent
    - Runner output stays compact and useful for local regression triage
  </acceptance_criteria>
  <done>The regression baseline now enforces the Phase 2 session/401 contract rather than the old mixed endpoint behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1 --auth-only`
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1`
- [ ] `rg -n "session_expired|sessionExpired|session_expired" D:\_Plana_Docker\Walllos_Remastered\scripts\api.js D:\_Plana_Docker\Walllos_Remastered\scripts\common.js D:\_Plana_Docker\Walllos_Remastered\scripts\subscriptions.js`
- [ ] Mirror changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos` with `docker compose -f docker-compose.yaml up -d --build --pull never`
- [ ] Verify `http://127.0.0.1:18282/health.php` returns `OK`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shared frontend request code owns session-failure recognition
- High-risk business modules stop hand-parsing raw 401 logic
- Regression runner enforces the Phase 2 endpoint contract
  </success_criteria>

<output>
After completion, create `.planning/phases/02-session-and-401-unification/02-03-SUMMARY.md`
</output>
