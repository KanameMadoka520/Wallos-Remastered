---
phase: 02-session-and-401-unification
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - includes/checksession.php
  - endpoints/media/subscriptionimage.php
  - endpoints/subscriptions/get.php
  - endpoints/payments/get.php
  - endpoints/subscription/paymenthistory.php
  - endpoints/subscription/getcalendar.php
autonomous: true
requirements:
  - SESS-01
  - SESS-02
user_setup: []
must_haves:
  truths:
    - Page requests reuse the shared backend auth/session helper while preserving redirect semantics.
    - High-risk async endpoints stop mixing raw text and inconsistent JSON session-failure payloads.
    - Media access reuses the shared auth/session core while preserving 403/404/429 binary-friendly behavior.
  artifacts:
    - includes/checksession.php
    - endpoints/media/subscriptionimage.php
    - endpoints/subscriptions/get.php
    - endpoints/payments/get.php
  key_links:
    - includes/checksession.php consumes shared auth/session primitives without breaking page redirect behavior
    - high-risk endpoints emit the standardized async session failure contract
    - media endpoint shares auth/session recovery code without collapsing into JSON output
---

<objective>
Migrate the highest-risk backend request paths onto the shared session/401 model.

Purpose: Apply the Phase 2 backend foundation to the page bootstrap, the most regression-prone endpoints, and the media access path where behavior differences matter most.
Output: Shared auth/session logic adopted by pages, high-risk endpoints, and media access with consistent failure contracts by request type.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-and-401-unification/02-CONTEXT.md
@.planning/phases/02-session-and-401-unification/02-01-PLAN.md
@includes/checksession.php
@endpoints/media/subscriptionimage.php
@endpoints/subscriptions/get.php
@endpoints/payments/get.php
@endpoints/subscription/paymenthistory.php
@endpoints/subscription/getcalendar.php
@tests/regression_runner.php
@tests/lib/regression_checks.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate page bootstrap to the shared auth/session helper</name>
  <files>includes/checksession.php</files>
  <read_first>includes/checksession.php, includes/auth_session.php, includes/header.php</read_first>
  <action>Refactor `includes/checksession.php` so it consumes the shared auth/session helper created in 02-01 instead of maintaining a separate cookie/session/token restoration implementation. Preserve current page semantics: logged-in pages still redirect unauthenticated users to `login.php`, trashed users still go through the existing redirect flow, and user/page context such as timezone and avatar setup still works. Do not leak JSON or raw helper payloads into page requests.</action>
  <verify>docker exec wallos-local php -l /var/www/html/includes/checksession.php</verify>
  <acceptance_criteria>
    - `includes/checksession.php` no longer owns a full duplicate of cookie/token restoration logic
    - Page redirect semantics remain page-oriented rather than JSON-oriented
    - Existing user-context setup still occurs after shared auth/session restoration
  </acceptance_criteria>
  <done>Page bootstrap uses the shared auth/session core without changing user-facing page redirect behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize high-risk async endpoint session failures</name>
  <files>endpoints/subscriptions/get.php, endpoints/payments/get.php, endpoints/subscription/paymenthistory.php, endpoints/subscription/getcalendar.php</files>
  <read_first>endpoints/subscriptions/get.php, endpoints/payments/get.php, endpoints/subscription/paymenthistory.php, endpoints/subscription/getcalendar.php, includes/validate_endpoint.php</read_first>
  <action>Migrate the most visible async endpoints onto the standardized session failure contract from 02-01. Remove the remaining raw text or structurally inconsistent session-expired responses in these high-risk endpoints, and make them emit the same machine-readable JSON failure shape while keeping their success payloads unchanged. Preserve endpoint-specific success behavior and avoid unrelated refactors.</action>
  <verify>docker exec wallos-local php -l /var/www/html/endpoints/subscriptions/get.php && docker exec wallos-local php -l /var/www/html/endpoints/payments/get.php</verify>
  <acceptance_criteria>
    - `endpoints/subscriptions/get.php` no longer returns raw session-expired text on protected failure
    - `endpoints/payments/get.php`, `paymenthistory.php`, and `getcalendar.php` align on the same session failure shape
    - Success payloads remain compatible with existing frontend expectations
  </acceptance_criteria>
  <done>The highest-risk async endpoints now fail in a consistent, machine-readable way when the session is invalid.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate media auth/session recovery without breaking binary HTTP behavior</name>
  <files>endpoints/media/subscriptionimage.php</files>
  <read_first>endpoints/media/subscriptionimage.php, includes/auth_session.php, includes/security_rate_limits.php</read_first>
  <action>Refactor `endpoints/media/subscriptionimage.php` to reuse the shared auth/session restoration logic from 02-01 while preserving media-specific HTTP behavior. Keep 403 / 404 / 429 as binary-friendly plain responses, keep rate-limit cookie signaling intact, and do not route media failures through generic JSON endpoint helpers. The goal is logic de-duplication and auth consistency, not output-shape homogenization.</action>
  <verify>docker exec wallos-local php -l /var/www/html/endpoints/media/subscriptionimage.php</verify>
  <acceptance_criteria>
    - Media endpoint no longer maintains its own full duplicate login restoration core
    - Media responses still differentiate 403 / 404 / 429 with non-JSON binary-friendly output
    - Existing rate-limit behavior remains intact
  </acceptance_criteria>
  <done>The media access path shares the auth/session core while preserving its specialized HTTP contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker exec wallos-local php -l /var/www/html/includes/checksession.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/subscriptions/get.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/payments/get.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/subscription/paymenthistory.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/subscription/getcalendar.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/media/subscriptionimage.php`
- [ ] Mirror changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos` with `docker compose -f docker-compose.yaml up -d --build --pull never`
- [ ] Verify `http://127.0.0.1:18282/health.php` returns `OK`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Pages, high-risk async endpoints, and media access no longer carry three different auth/session recovery cores
- High-risk async endpoints converge on one session failure JSON contract
- Media and page-specific failure semantics remain intact
  </success_criteria>

<output>
After completion, create `.planning/phases/02-session-and-401-unification/02-02-SUMMARY.md`
</output>
