---
phase: 03-observability-and-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/runtime_observability.php
  - endpoints/client/loganomaly.php
  - includes/request_logs.php
  - includes/security_rate_limits.php
autonomous: true
requirements:
  - OBS-01
user_setup: []
must_haves:
  truths:
    - Frontend/runtime anomalies can be recorded through an authenticated lightweight endpoint.
    - Existing anomaly/log systems ignore the self-reporting endpoint to avoid noise.
    - Maintainers can reuse the existing security anomaly browser to inspect new anomaly categories.
  artifacts:
    - includes/runtime_observability.php
    - endpoints/client/loganomaly.php
  key_links:
    - loganomaly endpoint writes through existing anomaly storage
    - runtime observability helper exposes Service Worker version parsing
---

<objective>
Build the backend observability foundation for lightweight runtime anomaly capture.

Purpose: Reuse existing anomaly infrastructure instead of inventing a new monitoring subsystem.
Output: A lightweight client anomaly logging endpoint plus helper functions for runtime observability data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-observability-and-feedback/03-CONTEXT.md
@admin.php
@includes/request_logs.php
@includes/security_rate_limits.php
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add runtime observability helper and anomaly endpoint</name>
  <files>includes/runtime_observability.php, endpoints/client/loganomaly.php</files>
  <read_first>includes/security_rate_limits.php, endpoints/admin/securityanomalies.php</read_first>
  <action>Create helper functions for runtime observability and a lightweight authenticated endpoint that records client runtime and request-failure anomalies into the existing anomaly store.</action>
  <verify>docker exec wallos-local php -l /var/www/html/includes/runtime_observability.php && docker exec wallos-local php -l /var/www/html/endpoints/client/loganomaly.php</verify>
  <acceptance_criteria>
    - anomaly endpoint accepts authenticated POSTs
    - runtime helper parses service worker cache versions
  </acceptance_criteria>
  <done>Backend observability foundation exists.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent self-generated logging noise</name>
  <files>includes/request_logs.php, includes/security_rate_limits.php</files>
  <read_first>includes/request_logs.php, includes/security_rate_limits.php</read_first>
  <action>Exclude the anomaly logging endpoint from noisy request/anomaly recursion and keep it compatible with existing logging/rate-limit behavior.</action>
  <verify>docker exec wallos-local php -l /var/www/html/includes/request_logs.php && docker exec wallos-local php -l /var/www/html/includes/security_rate_limits.php</verify>
  <acceptance_criteria>
    - anomaly logging endpoint is skipped by request log noise filters
    - anomaly logging endpoint does not cause avoidable recursion in rate-limit observability
  </acceptance_criteria>
  <done>Self-reporting endpoint no longer pollutes its own telemetry.</done>
</task>
</tasks>

<verification>
- [ ] `docker exec wallos-local php -l /var/www/html/includes/runtime_observability.php`
- [ ] `docker exec wallos-local php -l /var/www/html/endpoints/client/loganomaly.php`
- [ ] Mirror changed files into `D:\_Plana_Docker\Wallos_TCYSelf` and `D:\_Plana_Docker\Wallos`
- [ ] Rebuild `D:\_Plana_Docker\Wallos`
- [ ] Verify `health.php`
</verification>

<success_criteria>
- Backend anomaly capture exists
- Existing anomaly browser can be reused
  </success_criteria>

<output>
After completion, create `.planning/phases/03-observability-and-feedback/03-01-SUMMARY.md`
</output>
