---
phase: 03-observability-and-feedback
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - scripts/common.js
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - Frontend runtime errors and request failures are reported to the anomaly endpoint.
    - Session-expired flows give clearer feedback before reload.
    - Observability changes do not break the Phase 1 regression baseline.
  artifacts:
    - scripts/common.js
  key_links:
    - common.js reports anomalies to the backend endpoint
    - common.js provides clearer session-expired feedback
---

<objective>
Instrument the shared frontend layer for lightweight anomaly reporting and clearer failure feedback.

Purpose: Capture actionable client-side failures and improve user-visible feedback without changing the site's visual system.
Output: Frontend runtime/request anomaly reporting in the shared common layer plus clearer session-expired feedback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-observability-and-feedback/03-CONTEXT.md
@scripts/common.js
@.planning/phases/01-regression-safety-baseline/01-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add lightweight client anomaly reporting</name>
  <files>scripts/common.js</files>
  <read_first>scripts/common.js, endpoints/client/loganomaly.php</read_first>
  <action>Capture client runtime errors and request failures in the shared common layer, dedupe them, and submit them to the anomaly endpoint with low noise.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\common.js</verify>
  <acceptance_criteria>
    - common.js reports runtime and request anomalies without recursive spam
    - anomaly reporting skips session-expired/rate-limit/account-trashed noise
  </acceptance_criteria>
  <done>Shared frontend layer can report actionable anomalies to the backend.</done>
</task>

<task type="auto">
  <name>Task 2: Improve session-expired feedback while preserving existing flow</name>
  <files>scripts/common.js</files>
  <read_first>scripts/common.js</read_first>
  <action>Make session-expired handling show a clear toast/message before the automatic reload/login flow continues, without introducing a new notification system.</action>
  <verify>node --check D:\\_Plana_Docker\\Walllos_Remastered\\scripts\\common.js</verify>
  <acceptance_criteria>
    - session-expired event path gives explicit user feedback
    - existing theme/feedback styling remains in use
  </acceptance_criteria>
  <done>Session expiration is visibly communicated before the page reloads.</done>
</task>
</tasks>

<verification>
- [ ] `node --check scripts/common.js`
- [ ] `docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1`
- [ ] Mirror changed files into deployment copies and rebuild Wallos
- [ ] Verify `health.php`
</verification>

<success_criteria>
- Shared frontend anomaly reporting exists
- Session-expired feedback is clearer
- Regression baseline remains green
  </success_criteria>

<output>
After completion, create `.planning/phases/03-observability-and-feedback/03-03-SUMMARY.md`
</output>
