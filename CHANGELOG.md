# Wallos-Remastered 变更记录

## 说明

本文件记录 `Wallos-Remastered` 分支已经落地的重制能力摘要，不再沿用上游完整英文发布流水。

## 2026-04-24

- 回归检查：扩展 `tests/regression_runner.php`，新增静态契约检查，覆盖默认紫色主题、订阅页关键 DOM、订阅页脚本加载顺序、共享请求层、API key 传输约定和订阅图片大小显示槽位。
- 文档：新增 `下一步任务.md`，记录后续自动化回归、订阅页生命周期收敛、会话契约统一、可观测性和 SQLite 友好优化方向。

## 2026-04-25

- 订阅图片：修复未勾选压缩时原图仍被 GD 重新编码导致 PNG 文件膨胀的问题；现在未压缩上传会透传保存原始文件字节，预览图/缩略图仍会经过服务端校验与派生生成。
- 订阅图片：当生成出的预览图或缩略图没有降尺寸且比原图更大时，会复用原图作为该派生版本，避免“预览图比原图大”的反向压缩。
- 会话安全：页面后台挂起超过 30 分钟后回到前台，会提示刷新页面以重载 CSRF 安全令牌；后端返回 `invalid_csrf` 时也会显示同一套说明，避免只看到生硬的英文错误。
- 会话安全：页面底部部署信息区域新增本次页面 CSRF 安全令牌短指纹与预计过期时间，便于排查页面挂起后的令牌状态；完整令牌仍保持隐藏，避免截图泄露。

## 已完成的重要增强

### 管理后台

- 用户卡片式管理
- 用户 ID 展示与复制
- 管理员重置并生成用户临时密码
- 邀请码生命周期管理
- 封禁用户名单与计划删除时间管理
- 访问日志折叠展示
- 登录限速阈值后台可配置

### 安全

- 登录失败限速
- 请求日志清理
- 订阅图片直链拦截
- 备份直链拦截
- 鉴权媒体访问端点

### 订阅图片

- 多图上传
- 原图 / 预览图 / 缩略图分层
- 上传进度和处理进度
- 原图加载进度
- 拖拽排序
- 历史图片补生成派生图
- 删除主图时同步清理派生图

### 订阅页组织

- 顶部标签式订阅分页
- `所有 / 未分页 / 自定义分页` 切换
- 新增与编辑订阅时可调整所属分页
- 删除分页后订阅自动回到未分页

### 备份闭环

- 自动备份 `db + logos`
- 最近备份列表
- 手动备份与下载
- 备份校验
- 一键清理旧备份
- 直接从列表恢复

### 文档与公开仓库整理

- 中文 README
- 英文 README
- 中文贡献指南
- 中文安全策略
- 清理私有部署信息
- 统一项目命名为 `Wallos-Remastered`

### 2026-04 安全加固补充
- 收紧“禁用登录”旁路，仅允许本地直连请求自动进入管理员
- 空实例首次数据库恢复改为仅允许本地直连，并切换到统一备份校验/恢复链路
- API key 不再建议通过 URL 查询参数传递，改为优先使用请求头
- 加固支付方式图标外链抓取的重定向校验与地址约束
- 认证链路 cookie 在 HTTPS / 可信转发场景下补充 `Secure` 标记

