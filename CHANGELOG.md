# Wallos-Remastered 变更记录

本文件只记录 Remastered 分支的重要变化，不重复上游 Wallos 的完整发布流水。版本兼容基线是 `Wallos v5.4.5`，不代表本仓库与上游逐文件一致。

当前发布版本为 `v5.4.5-remastered.4`；`.1` 和 `.2` 是合并过程中保留的兼容里程碑，`.3` 是上一正式标签。下文的“验证”表示仓库增加或扩展了相应检查，不承诺所有硬件、代理和第三方魔改环境都天然无误。

## v5.4.5-remastered.4（2026-08-27）

这一版补齐最终兼容审计发现的上游 `v5.4.5` 能力缺口，并集中处理多语言缺词与部署文档漂移。

### 新增与兼容

- OIDC 支持通过 `OIDC_*` 环境变量声明式配置；环境值只在运行时覆盖对应数据库值，管理员页面会标记并锁定受环境管理的字段。
- 支持用 `OIDC_ISSUER` 读取 `/.well-known/openid-configuration`，同时允许认证、Token 和用户信息端点变量分别覆盖发现结果。
- 支持 `OIDC_CLIENT_SECRET_FILE` 从容器内文件读取客户端密钥，并优先于明文 `OIDC_CLIENT_SECRET`，方便接入 Docker Secret 或只读密钥挂载。
- 补入“在每个付款账期开始时发送摘要”的通知选项；默认关闭，启用后由既有定时通知流程按用户账期计算本期所需金额和剩余预算。
- 文本通知渠道通过共享消息构造器只附加一次账期摘要；自定义 Webhook 增加可选的 `{{period_summary}}` 占位符，未使用该占位符时保持原 payload 行为。
- 恢复上游的深浅主题 Logo 变体：开启去背景处理后，黑白文字型 Logo 会生成反色版本，订阅卡片与详情弹窗随主题切换。

### 多语言与界面修复

- PHP 与浏览器翻译层统一以英文词典兜底：所选语言缺少已知词条时显示英文，不再直接显示 `[i18n String Missing]` 或 `[Translation Missing]`；真正未知的 key 仍保留显式缺失提示，便于开发排查。
- 补齐账期、一次性购买、图片尺寸、订阅分页、OIDC、账期摘要和高级限速等近期界面的基准词条，并修复简繁中文词典中的粘连/缺项。
- 简体中文和繁体中文保持完整本地键集；其他语言尚未翻译的 Remastered 新词条由英文兜底，不冒充已经完成的本地翻译。
- 恢复阿拉伯语和匈牙利语入口及词典资源，Service Worker 同步预缓存对应浏览器词典。
- 修复订阅列表为空时排序结果数组未初始化的问题，避免“没有订阅”场景在 PHP 8 触发 `usort(null)` TypeError。
- 修复非强制汇率刷新把 `SQLite3Result` 当作日期传入 `DateTime` 的潜伏 TypeError；缺失或非法日期标记现在按“需要刷新”安全恢复。
- 修复 Logo 写盘失败仍报告成功、数据库事务失败遗留新 Logo，以及清理工具误删仍被订阅引用的主题变体。
- 修复完整备份校验器把 `logos/` 子目录条目误判为缺失的问题；数据库快照与 Logo/头像/订阅图片现在会在同一个短暂维护窗口内暂存，并逐文件完成尺寸与 SHA-256 校验，避免生成数据库引用和媒体文件不一致的“有效”归档。
- 修复 Docker bind mount 下恢复流程跨卷移动数据库、无法重命名 Logo 挂载点的问题；新数据库与媒体分别在自己的挂载卷内完整暂存、迁移和校验，再以可逆步骤联合切换。

### 安全与行为边界

- OIDC Issuer Discovery、Token 和用户信息端点继续经过 Remastered 的协议、SSRF、DNS 解析固定和内网 allowlist 边界，不因改用环境变量而绕过出站限制。
- 环境变量管理的 OIDC 启用状态、密码登录策略和配置字段不能被页面写回覆盖；先验证单点登录成功，再启用 `OIDC_DISABLE_PASSWORD_LOGIN`。
- 管理员页面和设置 API 不再回显有效 OIDC 客户端密钥，只展示遮罩和“已配置”状态；页面提交空密钥也不会擦除已有的非环境密钥。
- 内网身份提供商仍需明确配置管理员 SSRF allowlist 或 `SSRF_ALLOWLIST`；客户端密钥不应写进受版本控制的 Compose 文件。
- 备份校验现在要求归档文件集与 manifest 完全一致，拒绝重复/非规范路径、额外文件、符号链接、特殊文件、恢复保留目录和超限解压体积；备份创建同样不会跟随媒体符号链接。
- 恢复事务使用数据库卷内的持久 journal；提交前故障会同时回滚数据库和媒体，回滚不完整或提交后清理异常会保留维护状态并拒绝后续请求/启动，避免半恢复实例重新上线。
- 请求结束日志和其他运行时连接不再以 SQLite 默认创建模式绕过恢复锁；Nginx worker 也从 PHP 私有数据组移除，不能读取或改写数据库、WAL 与备份文件。

### 迁移与兼容

- 新增 `000080`：为 `notification_settings` 增加默认关闭的 `period_summary_at_period_start`，并为 `subscriptions` 补齐可空的 `logo_text_color`、`logo_variant`；已有通知设置、Logo 文件和自定义列值均不覆盖。
- 账期摘要复用 Remastered 已有的用户隔离汇率、特殊价格、一次性购买、排除统计和账期锚点逻辑，避免重新引入一套与统计页不同的估算口径。

### 验证

- 新增 `000080` 重复执行与数据保留测试，并把最新迁移及通知字段加入启动、正式数据库验证和健康检查契约。
- 新增声明式 OIDC 兼容测试，覆盖环境覆盖优先级、布尔值解析、Issuer Discovery、DNS 固定、Secret File 优先级、管理字段防覆盖和密钥脱敏。
- 新增账期摘要计算与通知测试，覆盖账期首日、特殊价格、一次性购买、排除统计、预算余额、只发送一次及 Webhook 占位符。
- 新增多语言静态契约，检查有效键集、重复/多余键、格式占位符、源码调用和资源加载顺序；另新增浏览器 E2E，逐个检查登录页语言、Missing 占位符、英文 JS 基线和阿拉伯语 RTL 方向。
- 新增空订阅列表排序回归，防止无数据页面再次使用未初始化的结果数组。
- 新增主题 Logo 生成/渲染/清理与迁移契约，以及非强制汇率刷新日期判断测试。
- 补充带嵌套媒体目录的完整备份 manifest 回归，并收紧管理员浏览器测试：备份校验显示错误时必须失败；空账户 E2E 也会选择真正可见的新增按钮。
- 增加数据库/媒体联合事务故障注入，覆盖候选复制、数据库切换、旧媒体移出、新媒体安装、提交前失败、回滚再失败和提交后故障；另用真实 Docker bind mount 验证成功恢复、失败回滚及恢复后启动。

### 文档

- 中英文 README 改为相同的精简结构，修正首次安装目录、数据库模板、严格挂载、升级、备份恢复和 OIDC 说明。
- 明确正式镜像不包含 `tests/`；回归 runner 示例改为在 Linux 主机用临时容器只读挂载源码，不再给出不可执行的生产容器路径。
- 删除基于浮动 `bellamy/wallos:latest` 的旧 `Dockerfile.local`，构建统一走固定摘要的正式 Dockerfile；同时让 Git 与镜像构建上下文排除根 `logos/` 真实媒体。

## v5.4.5-remastered.3（2026-08-27）

这是本轮上游等价升级后的正式标签，重点是让上游兼容逻辑与 Remastered 的账本、主题、媒体和运维能力共同工作。

### 新增与调整

- 新增账期预算，可按周、双周或锚定日期的月周期计算；填写 `0` 时保持关闭，不改变已有月预算和年预算。
- 统计页增加当前账期使用情况和未来 12 个月预测，并纳入实际付款、特殊价格、一次性购买和用户币种。
- 仪表盘的即将付款/逾期卡片可打开订阅详情弹窗，查看周期、付款方式、通知、备注和日历导出等信息。
- 日历、月度费用 API、统计、账本和续订写入共用更一致的日期与汇率计算逻辑。

### 修复

- 修复每月 29/30/31 日和 2 月 29 日续费逐月漂移的问题；短月份会临时收敛到月末，后续长月份仍回到原锚点日。
- 修复手动续订和定时更新下一付款日时使用不同算法的问题，并串行处理定时写入，降低重复推进和 SQLite 写冲突风险。
- 修复部分账本存在时记录、编辑或删除付款会错误改写当前到期日的问题；手动续订预测不再无限生成未来记录。
- 修复付款历史预测没有正确套用频率、锚定日期、一次性周期和特殊价格的问题。
- 修复用户汇率缺失、无效、为零或负数时可能产生错误换算的问题；无法可靠换算时保留原金额与币种语义。
- 修复公开注册初始化竞态、非 ASCII TOTP 标签编码和移动端维护动作表格溢出。

### 安全

- SMTP、Logo 搜索和支付图标搜索增加解析后地址固定、逐跳重定向检查及内网/保留地址限制，降低 DNS 重绑定和 SSRF 风险。
- 浏览器触发的 cron 校验入口改为要求 `POST`、管理员身份和 CSRF，不再把高权限动作暴露为普通页面访问。
- Web、媒体、健康检查和定时任务使用共享数据库运行锁；恢复操作使用独占锁，避免读取到只完成一半的数据。
- Nginx 阻断 `/db/`、`/backups/` 和临时恢复区域的静态访问；Service Worker 不缓存订阅私有媒体。

### 迁移与兼容

- 迁移改为逐项事务执行；失败时回滚当前迁移，不记录虚假的成功标记。
- 恢复流程先在隔离位置校验和迁移，再原子替换数据库与 Logo；替换失败时执行补偿回滚。
- 启动时先检查非空数据库和迁移清单，离线迁移、验证成功后才启动 Web 与 cron。正式 Compose 缺少数据库时会拒绝启动，避免挂载错误生成空实例。
- 新增的账期预算字段和订阅查询索引采用增量迁移；旧月/年预算、账号、订阅、付款记录、图片与备份保持原位。
- 历史迁移补强了对逐用户头像、通知归档、Webhook iterator、自定义用户组/状态、布局/转场扩展值及限速 JSON 扩展字段的保留。无法判定的语义冲突会停止迁移，而不是静默覆盖。
- Docker 基础版本和关键构建依赖固定；cron 只安装到一个调度位置，避免同一任务被两套配置重复触发。

### 验证

- 增加账期预算、12 个月预测、汇率、iCalendar、月底/闰日锚定、付款账本回算和订阅索引测试。
- 增加迁移账本完整性、旧自定义字段保留、恢复原子性、运行锁、启动拒绝空库、进程监管和 SSRF/SMTP 合约测试。
- 调整备份与客户端缓存浏览器测试的等待条件，减少页面尚未刷新完就被误判失败的竞态。

## v5.4.5-remastered.2（2026-08-26，兼容里程碑）

### 新增与兼容

- 增加一次性购买周期，并把它接入新增/编辑、列表、筛选、统计、日历、AI 建议和价格规则。
- 增加“每周从周日开始”的账号设置；默认仍沿用原有行为，不主动改动旧用户偏好。

### 修复

- 一次性购买只在真实购买日出现一次，不再进入周期续费、未来年度支出或重复通知。
- 今天到期的订阅会计入日历待付金额；一次性购买会尊重订阅开始日期。
- 成员、分类或付款方式筛选后的实付合计、明细和已付账期只统计当前命中的订阅，不再混入其他记录。
- 日历换算遇到零、负数或缺失汇率时回退原金额，避免除零和伪造的换算结果。

### 迁移与验证

- `000076` 仅在缺失时增加 ID 为 `5` 的一次性周期；若该 ID 已被第三方魔改占用且语义不同，迁移会明确失败，避免重新解释已有订阅。
- `000077` 只新增周起始日偏好，`000078` 只补订阅查询索引，均不删除业务记录。
- 扩展日历日期、一次性周期、统计筛选和迁移幂等性检查，同时保留 Remastered 的页面结构与主题契约。

## v5.4.5-remastered.1（2026-08-26，兼容里程碑）

### 上游兼容同步

- 以原有 Remastered 功能为主体，人工移植上游 `v5.4.5` 中适合本分支的用户隔离汇率、OIDC、TOTP、备份恢复、迁移和订阅查询修复。
- About 和版本接口分别显示 Remastered 版本与上游基线，避免把兼容升级误写成仍停留在 `v4.8.0`。
- 日历导出真正应用用户选择的币种转换，并按 iCalendar 规则转义订阅名称、备注、分类、付款方式、付款人和链接。

### 安全与修复

- 汇率更新按用户隔离，减少一个用户的配置或失败结果影响其他用户。
- OIDC 可要求已验证邮箱；TOTP 增加失败次数与锁定字段，并补强登录处理。
- 服务端外联地址校验吸收上游 SSRF、重定向和保留地址修复的语义，同时保留 Remastered 的管理员 allowlist 边界。
- 备份恢复和迁移入口增加更多结构检查；公开注册与管理员添加用户继续遵守本分支的默认值和权限规则。

### 迁移与验证

- `000075` 只补缺失的 TOTP/OIDC 字段、订阅索引和空 API key，不替换已有账号或订阅数据。
- 回归 runner 增加版本、iCalendar、用户隔离、OIDC/TOTP 和安全契约检查，为后续 `.2/.3` 的功能整合提供基线。

## 历史 Remastered 自研基线（2026-04-09 至 2026-05-26）

这一阶段从上游 `v4.8.0` 分出并逐步形成 Remastered。以下是按能力归纳的摘要，不是 300 余个提交的逐条流水。

### 用户可见功能

- 完善简体/繁体中文默认体验、公共页语言切换、人民币默认选项和可重置的本地化默认数据。
- 增加动态壁纸、毛玻璃、自定义主题/CSS、多套页面转场、沉浸背景查看，以及跟随账号的主题和布局偏好。
- 增加订阅分页、拖动排序、单/双/三列布局、回收站、Markdown 备注、实际付款账本、特殊价格、价值折算、年预算和解释弹层。
- 增加订阅多图上传、缩略图/预览图/原图、拖动排序、历史派生图补全和鉴权媒体访问。

### 管理与运维

- 增加邀请码、用户分组、封禁/延期删除、管理员临时密码重置、登录限速、访问日志和安全异常记录。
- 建立数据库 + Logo 自动/手动备份、清单校验、下载、保留期清理和后台恢复流程。
- 增加系统总览、慢请求排行、存储增长、SQLite 索引与维护、图片一致性审计、维护建议和管理员动作审计。
- 抽取共享请求层，统一会话失效、CSRF 错误和 SQLite 忙碌响应；逐步拆分订阅页与管理员页脚本，降低按钮和弹窗相互干扰。

### 安全与稳定性

- 阻断私有图片和备份静态直链；收紧公网“禁用登录”旁路、空实例首次恢复和 API key 的 URL 传递。
- 为页面 CSRF 令牌设置服务端 30 分钟有效期；日志内容进行安全转义，Service Worker 排除私有媒体并提供可见的缓存刷新提示。
- 人工移植上游 `4.8.1/4.8.3` 的关键 SSRF、DNS 重绑定、CGNAT 和私有页面输出修复，且没有覆盖本分支已有交互。
- 统一 `database_busy` 错误和常用 SQLite 索引，降低高并发写入时显示未知错误或长时间排队的概率。

### 验证基础

- 建立 PHP 回归 runner，覆盖公开页、登录态接口、迁移、预算、账本、图片、共享请求层和管理后台静态契约。
- 建立订阅、图片、客户端缓存和管理员后台 Playwright E2E；高权限测试要求显式且带过期时间的凭据。
- 测试和安全保护只能降低回归概率，生产升级仍需备份、查看日志和抽样恢复验证。

## 相关链接

- [当前部署与升级说明](README.md)
- [安全策略](SECURITY.md)
- [贡献与测试要求](CONTRIBUTING.md)
- [上游 Wallos Releases](https://github.com/ellite/Wallos/releases)
