# Wallos-Remastered

[English README](README_EN.md)

## 项目简介

`Wallos-Remastered` 是基于上游 `Wallos` 深度改造的重制分支，定位为更适合长期自托管、多用户受控使用、管理员集中运维与资源治理的版本。

这个仓库已经不再等同于官方 `Wallos` 默认行为。与官方版本相比，当前分支重点强化了以下方向：

- 更强的管理员后台
- 用户分组、邀请码与封禁名单管理
- 登录限速与访问日志治理
- 订阅图片多图上传、鉴权访问、缩略图/预览图/原图分层
- 自动备份、备份校验、后台直接恢复
- 更明确的源码构建式 Docker 使用方式

## 与官方 Wallos 的区别

官方 `Wallos` 常见用法是直接运行官方镜像。

`Wallos-Remastered` 的推荐方式不同：

- 推荐直接基于本仓库源码构建镜像
- 默认 compose 示例端口使用 `18282`
- 默认持久化目录包含：
  - `db`
  - `logos`
  - `backups`

也就是说，本仓库的默认 Docker 用法已经和官方仓库不再相同。

## 首次部署与初始管理员

首次部署时，如果数据库中还没有任何用户：

- 系统会自动进入注册流程
- 第一个注册成功的用户会成为初始管理员
- 当前系统默认将 `id = 1` 视为管理员账户

建议你在首次部署完成后，第一时间进入管理员后台确认以下设置：

- 注册是否开放
- 是否启用邀请码注册
- 登录限速阈值
- 订阅图片上传策略
- 备份保留天数

## 登录页与注册页语言

- 登录页默认简体中文
- 注册页默认简体中文
- 登录页和注册页都提供页面级语言切换器
- 注册时会自动把当前页面语言作为新用户的初始语言

## 推荐 Docker 用法

### 1. 克隆仓库

```bash
git clone https://github.com/KanameMadoka520/Wallos-Remastered.git
cd Walllos_Remastered
```

说明：

- 仓库目录名你可以自行调整
- 文档不再依赖任何私有本地文件夹结构
- 下文均默认你在仓库根目录执行命令

### 2. 直接源码构建并启动

仓库根目录已经提供源码构建版 `docker-compose.yaml`，直接执行：

```bash
docker compose up -d --build
```

### 3. 默认运行参数

当前仓库内置 compose 的默认行为如下：

```yaml
services:
  wallos:
    build:
      context: .
      dockerfile: Dockerfile.local
    image: wallos-remastered:latest
    container_name: wallos-remastered
    restart: unless-stopped
    ports:
      - "18282:80"
    environment:
      TZ: "Asia/Shanghai"
    volumes:
      - "./db:/var/www/html/db"
      - "./logos:/var/www/html/images/uploads/logos"
      - "./backups:/var/www/html/backups"
```

默认端口：

- `18282`

默认持久化目录：

- `./db`
- `./logos`
- `./backups`

## 健康检查

启动后可使用：

```bash
curl http://127.0.0.1:18282/health.php
```

正常情况下应返回：

```text
OK
```

## 当前已落地的重要增强

下面这部分不是“计划中功能”，而是当前仓库已经真实落地、并且正在围绕这些能力持续打磨的增强点。

### 1. 界面与主题增强

- 登录页、注册页默认简体中文，并支持页面级语言切换
- 登录页、注册页、主应用页支持统一的几何背景与动态渲染氛围
- 主题设置支持：
  - 浅色 / 深色 / 自动
  - 蓝 / 绿 / 红 / 黄 / 紫主题色
  - 自定义主色、强调色、悬停色、文字色
  - 自定义 CSS
- 动态壁纸当前内置方案：
  - `贴贴1`
- 动态壁纸支持：
  - 即时开关
  - 毛玻璃遮罩开关
  - 动态壁纸下专门的可读性适配
- 页面切换动画支持账号级开关与方案切换：
  - `切幕`
  - `蔚蓝档案`
  - `蔚蓝档案-跟随主题配色`
- 仪表盘页和订阅页额外提供“隐藏界面”按钮，便于只看背景和动态壁纸

### 2. 订阅页交互与排版增强

- 订阅列表支持：
  - 单列
  - 双列
  - 三列
- 多列模式下采用轻量 masonry 思路，尽量避免某一张展开卡片撑高整行
- 支持手动拖拽重排订阅顺序
- 支持搜索、筛选、排序并与多列布局共存
- 支持“订阅分页”标签页，可把大量订阅拆分到不同分页中查看，并可在新增/编辑时调整所属分页
- 订阅卡片的“价值信息显示”和“实际支付记录显示”偏好可跟随账号保存
- 订阅页支持独立的回收站垃圾桶入口，不再默认长期占据页面正文

### 3. 实际支付记录 / 账本体系

- 每个订阅可维护独立的实际支付记录
- 历史记录支持：
  - 查看
  - 编辑
  - 删除
  - 导出 CSV
  - 导出 JSON
- 账本视图支持：
  - 账本记录
  - 年度现金流
  - 未来预测
- 新建订阅时会自动补第一笔实际支付记录
- 手动“续订”时会自动补一笔实际支付记录并推进下一期
- 手动删除支付记录时，不回滚已经推进的续期
- 订阅卡片支持显示：
  - 累计已投入成本
  - 剩余价值折算
  - 本周期已使用价值

### 4. 价格规则与预算增强

- 支持订阅特殊价格规则：
  - 前 N 个账期优惠
  - 日期区间优惠
  - 单次特价
- 未来预测和账本说明会回放当前规则来源
- 首页 / 统计页已经接入：
  - 本月实付
  - 本年实付
  - 本年预计总支出
- 多项预算和成本指标支持解释弹层，可查看计算公式与组成

### 5. 订阅图片与媒体治理

- 每个订阅支持多张服务器图片
- 图片分层为：
  - 原图
  - 预览图
  - 缩略图
- 未勾选“压缩上传到服务器的图片”时，原图按上传文件原始字节透传保存；勾选压缩时，保存的原图会先经过一次压缩处理
- 订阅列表默认加载缩略图
- 图片预览弹窗默认加载预览图
- 只有点击“打开原图”时才拉取高质量文件
- 支持上传进度、处理进度、原图加载进度
- 支持拖拽排序
- 支持历史图片一键补生成缩略图与预览图
- 删除图片时会连带清理派生图
- 图片预览弹窗会显示缩略图 / 预览图 / 原图三层文件大小
- 选择本地图片后，添加/编辑订阅表单会在上传前显示本地文件大小
- 当预览图或缩略图生成后反而大于原图时，会复用原图作为该层资源，避免无意义膨胀
- 上传策略明确区分：
  - 未勾选上传压缩：原图按上传文件原始字节透传保存，仅做格式/解码能力校验
  - 勾选上传压缩：保存的原图先进行一次上传压缩
  - 预览图/缩略图：始终作为展示层单独生成，且不会无限重复压缩
- 服务器图片与外链图片分开处理：
  - 外链图片不会下载到本地服务器
  - 服务器图片受鉴权访问保护

### 6. 管理员后台与多用户治理

- 用户卡片式管理
- 用户 ID 展示与复制
- 管理员一键重置并生成用户临时密码
- 邀请码管理支持：
  - 正在使用
  - 已删除
  - 彻底删除
- 用户封禁名单 / 计划删除时间管理
- 访问日志可折叠查看
- 登录限速阈值后台可配置
- 顶部版本说明文字可在管理员后台编辑

### 7. 回收站与删除保护

- 删除订阅时优先进入订阅回收站
- 订阅回收站支持：
  - 恢复
  - 彻底删除
  - 自动保留期
- 用户删除改为进入封禁/回收区，而不是直接硬删
- 删除图片时同步清理缩略图、预览图、原图

### 8. 安全与运维闭环

- 登录失败限速
- CSRF 页面安全令牌服务端强制 30 分钟 TTL；页面挂起过久或提交过期表单时会提示刷新页面
- 页脚只显示 CSRF 令牌短指纹，不显示完整令牌
- 请求日志记录与清理
- 图片直链受保护
- 备份直链受保护
- 自动备份 `db + logos`
- 后台显示最近备份列表
- 手动备份、下载、校验、清理旧备份
- 直接从备份列表恢复
- 备份、图片、媒体访问全部尽量走鉴权端点而不是裸静态目录
- 管理员后台显示 Service Worker 缓存版本、当前浏览器控制状态，并可发布客户端缓存刷新提示
- 管理员后台提供维护入口：
  - 查看日志/异常/速率限制用量保留策略
  - 扫描订阅图片孤儿文件和缺失衍生图记录
  - 一键将大于原图的预览图/缩略图改为复用原图，并清理不再被引用的过大派生文件
  - 手动执行 SQLite `PRAGMA optimize` / `ANALYZE` / `VACUUM`

### 9. 日历增强

- 日历页支持深色动态壁纸适配
- 日历页不再只能点“上个月 / 下个月”
- 现在支持直接切换年份和月份，并立即刷新
- 保留后端对过去月份访问的保护逻辑

### 10. 账号级个性化持久化

以下设置已经不只是保存在本地浏览器，而是可以跟随账号：

- 主题模式
- 主题颜色
- 动态壁纸开关
- 动态壁纸毛玻璃开关
- 页面切换动画开关与方案
- 订阅页列数偏好
- 订阅页价值信息显示偏好
- 订阅页图片布局偏好

## 备份与恢复

### 自动备份

- 自动备份内容包含：
  - 数据库
  - 图片与 logos 目录
- 默认备份目录：
  - `./backups`

### 后台支持

管理员后台支持：

- 创建备份
- 下载备份
- 校验备份
- 清理旧备份
- 直接恢复所选备份

### 安全边界

- `/backups/` 静态目录已由 Nginx 拦截
- 备份必须通过后台鉴权端点访问

## 仓库发布注意事项

本开源仓库版本已经尽量移除或隔离以下内容：

- 私有部署目录结构
- 自建节点说明
- 自建版横幅文案
- 私有域名和环境特有描述
- 部署专用内部交接文档

另外，本仓库已通过 `.gitignore` 和 `.dockerignore` 尽量避免纳入以下运行期内容：

- 本地 `.env`
- 临时目录
- 日志
- 运行期数据库
- 运行期图片
- 运行期备份文件

## 文档索引

- 中文说明：`README.md`
- 英文说明：`README_EN.md`
- 防刷站指南：`FRP+Nginx+Fail2ban防刷站部署指南.md`
- 贡献指南：`CONTRIBUTING.md`
- 变更记录：`CHANGELOG.md`
- 安全策略：`SECURITY.md`
- 共享契约文档：`docs/共享请求层与稳定性契约.md`

## 共享请求层与稳定性约定

为避免后续继续开发时再次出现“同类请求每个页面各写一套”的退化，本仓库现在默认遵循以下约定：

- 高频页面请求优先复用 `WallosApi` / `WallosHttp`
- 原始 `fetch` 只建议保留给少量特殊流：
  - 文本型 cron 输出
  - 下载/导出
  - 纯二进制媒体流
- 会话失效与通用错误反馈应优先复用共享请求层和 `common.js`
- 修改高风险链路后，建议至少运行一次：

```bash
docker exec wallos-local php /var/www/html/tests/regression_runner.php --base-url=http://127.0.0.1
```

当前回归 runner 会覆盖：

- `health.php`
- 登录页 / 注册页主题与默认紫色主题
- Service Worker 缓存契约
- 未登录 endpoint 的标准 `401` JSON 契约
- 订阅分页 JSON 契约
- 订阅页关键 DOM 与脚本加载顺序
- 订阅页共享请求层、会话失效处理和事件重绑契约
- API key Header / POST 优先传输契约
- 订阅图片缩略图、预览图、原图大小显示契约
- 既有预算、账本、偏好逻辑回归

登录态回归可以使用专用测试账号运行：

```bash
docker exec wallos-local php /var/www/html/tests/regression_runner.php \
  --base-url=http://127.0.0.1 \
  --username=你的测试账号 \
  --password=你的测试密码
```

如需验证真实写入链路，可以追加 `--mutating-auth-checks`。该模式会创建一个临时订阅，随后完成编辑、记录支付、移入回收站和彻底删除，用于确认新增/编辑/支付记录/删除链路没有漂移：

```bash
docker exec wallos-local php /var/www/html/tests/regression_runner.php \
  --base-url=http://127.0.0.1 \
  --username=你的测试账号 \
  --password=你的测试密码 \
  --mutating-auth-checks
```

订阅页还提供浏览器级 E2E 脚本，适合在修改订阅页按钮、弹窗、分页、主题和图片预览后运行：

```bash
npm install
WALLOS_BASE_URL=http://127.0.0.1:18282 \
WALLOS_TEST_USERNAME=你的测试账号 \
WALLOS_TEST_PASSWORD=你的测试密码 \
npm run e2e:subscriptions
```

该脚本会真实打开浏览器并点击订阅分页、三点菜单、编辑弹窗、新增订阅保存、实际支付记录、记录支付弹窗、图片预览、单列/双列/三列切换、成本与价值显示开关、动态壁纸下的隐藏界面按钮和 CSRF 持久提醒。脚本创建的临时订阅会在检查结束时自动清理，并会把测试前的订阅页显示偏好恢复回去。

如果浏览器级 E2E 失败，脚本会把截图、当前 HTML 和诊断 JSON 写入 `screenshots/e2e/`。诊断内容包含前端 `console.error`、页面运行时异常、失败请求和 endpoint 异常响应，便于定位“按钮点不了”“分页只转圈”“弹窗打不开”这类回归。

也可以直接运行全部浏览器级检查：

```bash
npm run e2e
```

如果你准备继续维护本仓库，请优先阅读：

- `CONTRIBUTING.md`
- `docs/共享请求层与稳定性契约.md`

## 许可证

本分支继续遵循上游项目的 GPLv3 许可证。

## 2026-04 安全行为变更

以下行为已被安全加固，主要用于避免公网部署、FRP 穿透、反向代理或 VPS 网关场景下出现误暴露：

- “禁用登录”不再等同于“任何访问者自动进入管理员”。该旁路现在只允许真正的本地直连请求触发。
- 如果服务是通过公网域名、FRP、反向代理或带转发头的链路暴露出去，请继续使用正常登录流程，不要依赖“禁用登录”作为快捷入口。
- 空实例的首次数据库恢复现在只允许本地直连访问执行，避免新部署实例被外部请求抢先导入备份并接管。
- API key 不再建议通过 URL 查询参数传递。`api/*` 相关接口应优先改用 `X-API-Key` 或 `Authorization: Bearer ...` 请求头，避免密钥进入浏览器历史、代理日志与访问日志。
- 支付方式图标的外链抓取逻辑已经补上逐跳校验和重定向约束；如果某些外链图标地址会跳转到异常地址或私网资源，可能会被拦截，这是预期的安全行为。

如果你当前的访问链路类似：本地服务 -> FRP -> VPS -> `wallos.tcymc.space`，请将该实例视为公网可达服务，而不是单纯的本地站点。

